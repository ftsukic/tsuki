import React from 'react'

import { Text, View } from 'react-native'

/**
 * @title Common hooks
 * @description Shared hooks provide controlled state and lifecycle helpers.
 */
export default function HooksBasicFixture() {
  return (
    <View style={{ padding: 16 }}>
      <Text>Common hooks can be composed in custom components.</Text>
    </View>
  )
}
