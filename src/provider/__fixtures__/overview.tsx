import React from 'react'

import { Text } from 'react-native'

/**
 * @title Provider overview
 * @description Provider supplies the application-level context and portal host.
 */
export default function ProviderOverviewFixture() {
  return <Text style={{ padding: 16 }}>Provider 已挂载</Text>
}
