import { Skeleton } from '@ftsukic/tsuki'

/**
 * @title Basic
 * @description Render a title and paragraph rows with Vant-style defaults.
 */
export default function SkeletonBasicExample() {
  return <Skeleton title row={3} />
}
