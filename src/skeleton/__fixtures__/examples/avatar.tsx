import { Skeleton } from '@ftsukic/tsuki'

/**
 * @title Avatar
 * @description Combine an avatar placeholder with content placeholders.
 */
export default function SkeletonAvatarExample() {
  return <Skeleton avatar title row={2} />
}
