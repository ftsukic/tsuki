import React from 'react'

import { Text, View } from 'react-native'

/**
 * @title Helpers overview
 * @description Helpers are utility functions and do not render a visual component.
 */
export default function HelpersOverviewFixture() {
  return (
    <View style={{ padding: 16 }}>
      <Text>Helpers are available to component implementations.</Text>
    </View>
  )
}
