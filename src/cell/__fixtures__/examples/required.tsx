import { Cell } from '../../..'

/**
 * @title Required
 * @description A Cell with a required marker.
 */
export default function CellRequiredFixture() {
  return <Cell title="姓名" required value="请输入" />
}
