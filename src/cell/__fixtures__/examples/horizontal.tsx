import { Cell } from '../../..'

/**
 * @title Horizontal
 * @description 默认 horizontal Cell 将 title/value 放在 Main 的两列中。
 */
export default function CellHorizontalFixture() {
  return <Cell title="联系人" value="138 0000 0000" valueAlign="right" />
}
