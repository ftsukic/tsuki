import { Cell } from '../../..'

/**
 * @title Label
 * @description A Cell with secondary label text under its title.
 */
export default function CellLabelFixture() {
  return <Cell title="账号" label="用于登录和身份验证" value="已绑定" />
}
