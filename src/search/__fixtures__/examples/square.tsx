import { Search } from '../../..'

/**
 * @title Square
 * @description Use square shape for a search field with a small radius.
 */
export default function SearchSquareFixture() {
  return <Search shape="square" placeholder="搜索列表" />
}
