import { Search } from '../../..'

/**
 * @title Input align
 * @description Center the search text without moving the search icon.
 */
export default function SearchAlignFixture() {
  return <Search inputAlign="center" placeholder="请输入搜索关键词" />
}
