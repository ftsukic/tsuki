import { Search } from '../../..'

/**
 * @title Background
 * @description Customize the outer Search background independently from its input surface.
 */
export default function SearchBackgroundFixture() {
  return <Search background="#eaf3ff" shape="round" placeholder="搜索联系人" />
}
