import { Search } from '../../..'

/**
 * @title Disabled
 * @description Render a disabled Search input with its current value.
 */
export default function SearchDisabledFixture() {
  return <Search disabled value="当前不可搜索" />
}
