import { Divider } from '../../..'

/**
 * @title Basic Divider
 * @description Render a horizontal hairline with the theme defaults.
 */
export default function DividerBasicFixture() {
  return <Divider />
}
