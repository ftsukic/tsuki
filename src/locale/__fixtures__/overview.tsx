import React from 'react'

import { Text, View } from 'react-native'

/**
 * @title LocaleProvider overview
 * @description Locale configuration supplies default component messages.
 */
export default function LocaleOverviewFixture() {
  return (
    <View style={{ padding: 16 }}>
      <Text>默认语言：中文</Text>
    </View>
  )
}
