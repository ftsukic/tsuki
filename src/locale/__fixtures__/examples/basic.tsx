import React from 'react'

import { Text, View } from 'react-native'

/**
 * @title Local language
 * @description Locale values can be overridden for a subtree.
 */
export default function LocaleBasicFixture() {
  return (
    <View style={{ padding: 16 }}>
      <Text>局部语言配置</Text>
    </View>
  )
}
