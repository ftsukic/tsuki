import { Navbar } from '../../..'

/**
 * @title Long title
 * @description Keep a long title centered between independently positioned actions.
 */
export default function NavbarLongTitleFixture() {
  return <Navbar title="这是一个较长的导航标题，用来展示标题始终保持居中" />
}
