import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'React Native UI',
  outputPath: 'dist',
  favicons: [],
  themeConfig: {
    name: 'React Native UI',
    logo: false,
  },
});
