/**
 * @title Loading · text
 * @description 展示 Loading 的真实公开 API 和可交互状态。
 */
import { Loading } from '@ftsukic/react-native-ui'

export default function Example() {
  return <Loading type="circular" />
}
