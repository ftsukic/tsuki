/**
 * @title TextInput 基础用法
 * @description 使用 clearable 和 prefix。
 */
import { TextInput } from '@ftsukic/react-native-ui'

export default function Example() {
  return <TextInput placeholder="请输入内容" clearable prefix="姓名" />
}
