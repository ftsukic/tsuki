# Snack examples

这里保存唯一来源的 Expo Snack 示例源码。

建议按组件和场景组织：

```text
examples/snack/
└── button/
    ├── basic.tsx
    ├── disabled.tsx
    └── loading.tsx
```

`0.0.1` 只建立目录约定，不创建具体组件 demo，也不生成 saved Snack。
