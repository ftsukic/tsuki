---
'@ftsukic/tsuki': major
---

Remove the public `LoadingIcon` and `LoadingIconProps` APIs. Make `Loading` the canonical loading component and add Vant-style `loadingType` and `loadingSize` props to Button.
